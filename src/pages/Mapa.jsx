import { Typography, Box } from '@mui/material';

export default function Mapa() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Mapa SIG</Typography>
      <Typography variant="body1">Página en construcción.</Typography>
    </Box>
  );
}
